For wireless communication of data transmission, to build our own LoRa network, ChirpStack open-source LoRaWAN Network was considered. The proposed 
On that contrast ChirpStack was as following 
configured 
once in gateway and again in AWS EC2 as aplication server. But it was always found that "Gateway never seen" in web server. 
